name: test
runtime: yaml
description: A minimal Google Cloud Pulumi YAML program
outputs:
  bucketName: ${my-bucket.name}
resources:
  my-bucket:
    properties:
      location: northamerica-northeast1
      name: artifact-registry-371104-test_bucket
    type: squid:index:GCPBucket
