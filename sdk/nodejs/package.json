{
    "name": "@pulumi/squid",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource squid ${VERSION}"
    },
    "dependencies": {
        "@pulumi/gcp": "^6.0.0"
    },
    "devDependencies": {
        "typescript": "^4.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
